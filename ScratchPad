let
    // Create a list with 59 null values
    Source = Table.FromList(
        List.Repeat({null}, 59), // Create 59 rows
        Splitter.SplitByNothing(), 
        {"IP Range"}, // Column name
        null, 
        ExtraValues.Error
    )
in
    Source
