WVDConnections
| where TimeGenerated >= ago(30d)                       // Filter for data from the last 30 days
| extend UserName = trim(" ", tolower(UserName))        // Trim whitespace and convert UserName to lowercase
| distinct UserName                                     // Select distinct UserNames
